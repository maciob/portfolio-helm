apiVersion: v1
kind: Service
metadata:
  name: {{ include "logs.fullname" . }}
  labels:
    {{- include "logs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "logs.selectorLabels" . | nindent 4 }}
